/**
 * Explicit case registration and per-case failure evidence. Public suite records assemble
 * package-private case records; data-driven cases are ordinary instances. This contract has
 * no dependency on the logging service and does not change the v01 annotation-based API.
 */
package work.archaic.service.test.v02;
