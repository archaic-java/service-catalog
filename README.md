# Archaic Work Contracts

To work archaic means not to depend on third party code if feasible. It does not mean to never use libraries. But these libraries have to comply to contracts defined in this repo.

Applications depend on contracts describing services implemented by providers. This repo holds all contracts formulated with archaic.work practices in mind.
